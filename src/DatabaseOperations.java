interface DatabaseOperations{
    void insert();
    void display();
    // Note: update(), delete(), and search() methods are implemented by all classes
    // but not defined in this interface for flexibility
}